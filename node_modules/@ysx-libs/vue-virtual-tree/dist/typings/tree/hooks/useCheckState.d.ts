import { KeyNodeMap, NodeKey } from '../types';
import { BaseTreeNode } from '../baseTreeNode';
interface CurrentState {
    checkedKeys: Set<NodeKey>;
    halfCheckedKeys: Set<NodeKey>;
    checkStrictly: boolean;
    key2TreeNode: KeyNodeMap;
}
declare function useCheckState(defaultCheckedKeys: NodeKey[], { checkedKeys, halfCheckedKeys, checkStrictly, key2TreeNode }: CurrentState): void;
declare function updateCheckedState(options: {
    node: BaseTreeNode;
    checked: boolean;
    checkedKeys: Set<NodeKey>;
    halfCheckedKeys: Set<NodeKey>;
    key2TreeNode: KeyNodeMap;
    checkStrictly: boolean;
}): void;
export { useCheckState, updateCheckedState };
